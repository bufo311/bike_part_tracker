# Superseded by deploy.yml — kept as a placeholder so git history is clean.
# This workflow has no automatic triggers and will never run on its own.
name: (disabled) Old static deploy

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "This workflow is disabled. Use deploy.yml instead."
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is intentionally disabled. See deploy.yml."

name: Supabase Keep-Alive Ping

on:
  schedule:
    - cron: '0 0 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          curl -X PATCH "https://bjjagqcvcpxjambklagd.supabase.co/rest/v1/keep_alive?id=eq.1" \
            -H "apikey: sb_publishable_TTU01Ke9TYOZG5uiEaOU1A_Ge9X2o9w" \
            -H "Authorization: Bearer Ysb_publishable_TTU01Ke9TYOZG5uiEaOU1A_Ge9X2o9w" \
            -H "Content-Type: application/json" \
            -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64)" \
            -d '{"ping": "bot_was_here"}'
